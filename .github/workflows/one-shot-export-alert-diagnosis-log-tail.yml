name: Export alert diagnosis log tail

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/one-shot-export-alert-diagnosis-log-tail.yml

permissions:
  actions: read
  contents: write

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Export failed job log tail
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p diagnostics
          gh api repos/${GITHUB_REPOSITORY}/actions/jobs/89117526644/logs > /tmp/job.log
          tail -n 220 /tmp/job.log > diagnostics/loading-alert-job-tail.txt
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add diagnostics/loading-alert-job-tail.txt
          git commit -m "Record alert diagnosis failure log tail"
          git pull --rebase origin main
          git push origin HEAD:main

name: Hold after final GG 2.0.5 validation trigger

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/v2-build-apks.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hold:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Final GG 2.0.5 validation was triggered by the previous workflow revision.'

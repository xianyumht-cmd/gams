name: Calibrate latest page target entry

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: latest-page-target-entry-calibration-20260804
  cancel-in-progress: true

jobs:
  calibrate:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    env:
      OUTPUT_DIR: latest-page-target-entry-calibration
      CURRENT_FIRST_PATH: remote-script/src/noname.js
      CURRENT_SECOND_PATH: game-engine/release/game-1.0.5.js
      CURRENT_SECOND_SIZE: 11590534
      CURRENT_SECOND_SHA256: ba8103ab7ef8ee8327df6d1d73daba8aca3a4c90d2370e30ab93001fce289112

    steps:
      - uses: actions/checkout@v4
        with:
          ref: diagnostics/target-entry-calibration-20260804
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Verify target-entry calibration boundary
        shell: bash
        run: |
          set -euo pipefail
          node --check docs/diagnostics/run_latest_page_target_entry_calibration.mjs
          test -s "$CURRENT_FIRST_PATH"
          grep -Fq '@version      1.1.4' "$CURRENT_FIRST_PATH"
          test "$(stat -c%s "$CURRENT_SECOND_PATH")" = "$CURRENT_SECOND_SIZE"
          test "$(sha256sum "$CURRENT_SECOND_PATH" | awk '{print $1}')" = "$CURRENT_SECOND_SHA256"
          test ! -e scripts/apply_latest_page_compat_candidate.py
          test ! -e scripts/run_latest_page_compat_acceptance.mjs
          test ! -e .github/workflows/test-latest-page-compat-candidate.yml

      - name: Install Chromium
        shell: bash
        run: |
          set -euo pipefail
          npm install --no-save --no-package-lock playwright@1.52.0
          npx playwright install --with-deps chromium

      - name: Capture page-specific target entries
        id: calibration_run
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          node docs/diagnostics/run_latest_page_target_entry_calibration.mjs 2>&1 | tee /tmp/latest-page-target-entry-calibration.log

      - name: Build target-entry status
        if: always()
        shell: bash
        env:
          CALIBRATION_OUTCOME: ${{ steps.calibration_run.outcome }}
        run: |
          set -euo pipefail
          mkdir -p "$OUTPUT_DIR"
          test -f /tmp/latest-page-target-entry-calibration.log && cp /tmp/latest-page-target-entry-calibration.log "$OUTPUT_DIR/workflow.log" || true
          python3 - <<'PY'
          import json
          import os
          from datetime import datetime, timezone
          from pathlib import Path

          output_dir = Path(os.environ["OUTPUT_DIR"])
          report_path = output_dir / "report.json"
          report = json.loads(report_path.read_text(encoding="utf-8")) if report_path.exists() else None
          capture_ok = bool(report) and bool(report.get("captureOk")) and os.environ.get("CALIBRATION_OUTCOME") == "success"
          status = {
              "schemaVersion": 1,
              "generatedAt": datetime.now(timezone.utc).replace(microsecond=0).isoformat().replace("+00:00", "Z"),
              "runId": int(os.environ["GITHUB_RUN_ID"]),
              "state": "completed",
              "workflowOutcome": os.environ.get("CALIBRATION_OUTCOME", "unknown"),
              "captureOk": capture_ok,
              "targetEntryVisualValidated": False,
              "ok": False,
              "mode": "page-specific-target-entry-calibration-capture-only",
              "summary": report.get("summary") if report else None,
              "reportAvailable": bool(report),
              "artifactName": "latest-page-target-entry-calibration",
              "apkBuilt": False,
              "productionDefaultChanged": False,
              "runtimeFilesChanged": False,
              "androidClientChanged": False,
              "authorizationOutcomeModified": False,
              "paymentCompleted": False,
          }
          Path("/tmp/LATEST_PAGE_TARGET_ENTRY_CALIBRATION_STATUS.json").write_text(
              json.dumps(status, ensure_ascii=False, indent=2) + "\n",
              encoding="utf-8",
          )
          print(json.dumps(status, ensure_ascii=False, indent=2))
          PY

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: latest-page-target-entry-calibration
          path: latest-page-target-entry-calibration
          retention-days: 30
          if-no-files-found: warn

      - name: Publish target-entry status to main
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin main --quiet
          rm -rf /tmp/latest-page-target-entry-status
          git worktree add -B latest-page-target-entry-status /tmp/latest-page-target-entry-status origin/main
          cd /tmp/latest-page-target-entry-status
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          mkdir -p docs
          cp /tmp/LATEST_PAGE_TARGET_ENTRY_CALIBRATION_STATUS.json docs/LATEST_PAGE_TARGET_ENTRY_CALIBRATION_STATUS.json
          git add docs/LATEST_PAGE_TARGET_ENTRY_CALIBRATION_STATUS.json
          if ! git diff --cached --quiet; then
            git commit -m "Record latest page target entry calibration status"
            git push origin HEAD:main
          fi
          cd "$GITHUB_WORKSPACE"
          git worktree remove --force /tmp/latest-page-target-entry-status

      - name: Enforce capture gate
        if: always()
        shell: bash
        run: |
          jq -e '.captureOk == true and .targetEntryVisualValidated == false and .apkBuilt == false' /tmp/LATEST_PAGE_TARGET_ENTRY_CALIBRATION_STATUS.json >/dev/null

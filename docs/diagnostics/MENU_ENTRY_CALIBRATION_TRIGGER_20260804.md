# Menu-entry calibration trigger

This branch marker triggers the capture-only common menu-entry calibration. It does not change runtime files, Android client code, the production default channel or authorization outcomes.
